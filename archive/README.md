# archive — 归档区

废弃代码、旧资源、实验、手动备份放这里。**不要删除历史实现，也不要留在 `src/` / `public/` 让搜索误伤。**

```
archive/
├─ old_versions/   旧版源码（如 src_old_YYYYMMDD）
├─ old_assets/     不再引用的图/视频
├─ experiments/    一次性实验
└─ backups/        手动备份
```

Agent：默认不读、不 Grep 本目录。当前这些子目录是空的，等有废弃物再移入。
