# 修改记录（短）

详细过程只追加到 `docs/history.md`，本文件保持最近若干条。

## 2026-08-26

- **本幕舞台加厚**：最多 3 立绘（左中右默认位）；滚轮/± 缩放；双击改角色名/台词/场景字；本幕转场淡入/黑场/无。播放同步 `figures` + `transition`。
- **本幕舞台（所见即所得）**：编辑器右侧预览画布；点积木即时看背景/立绘/对白/选项；可拖立绘与场景字；新增 `figure` 字段，播放同步。补 VN/视频交互可视化短板，不迁 Cocos。
- **定案：分发与消费** — 站内消费固定；作品网页模式分享；远景个人主页（店）+ 生命市场（市场）可有 App。ZIP/多端包非当前目标。写入 `MASTER_SPEC` / `ARCHITECTURE` / `editor-vision`。
- **首页背景视频 · 夸克/UC**：去掉 `life.mp4` 音轨；补 X5 内联属性；去掉 HTML `autoplay`，JS 静音后再 play——避免国内浏览器劫持成全屏有声播放器盖住世界选择页。
- **分支图 + 播放存档 + 访问标记**：编辑器「分支图」标死端/孤儿/跳转；播放 3 槽本机存读档；进入积木自动 `v_<id>=1` 供 require。对标 Tuesday/Kinexus/WebGAL 强项（不迁引擎）。
- **表现层 perf 积木**：预设台词；`require` 不满足则跳过；可选「临场一句」`POST /api/hub/live-line`（失败回退预设，不改剧情）。
- **剧情变量 + 选项条件/效果**：`logic.state`；选项 `require`（隐藏）/`effect`（落账）；播放顶栏显示变量；样品「路边」可演示车站支线。零 LLM。
- **定案：作品三层模型** — 逻辑程序控 / 剧情作者控 / 表现 AI 可选；AI 不决定剧情。写入 `MASTER_SPEC` / `ARCHITECTURE` / `editor-vision`。
- **AI 中枢前端**：`brain.html` 一句话 → 企划人审 → 生成画面+配音或只导入文字骨架 → 写入作品编辑器；幻想页入口；`composeStoryJSON` 输出真实 `choice` 积木。
- **作品素材库**：本机 `hyool_assets_v1` 只存 URL 引用；侧栏管理 + 积木从库选用；上传自动入库。

- 互动小说：新增 **选项（choice）积木**——编辑提示与跳转（下一块 / 结束 / 指定积木 / 章节开头）；播放时点选分支；样品「路边」含双结局示范。

## 2026-08-25

- 本机 ComfyUI 全站生图：浏览器直连 `:8000`；内置 ZIT「国风首选」与 krae2「画面优质」；挂到创角/重绘、主页与世界封面、作品立绘、无限工具箱；云端 Pollinations 仍作回退。须 http 本地打开本站。

- 女神挂机：召唤卡池（单抽/十连）+ 角色升级/升星；战斗吃等级与星级。

- 女神挂机第一版：试玩进入主城壳（立绘阵容 / 女神册 / 关卡 / 挂机推关）；工作室支持角色立绘上传；通关累计金币与关卡进度。
- 作品编辑器傻瓜式大翻新：作品库三步创建；剪映式时间线（拖排序、文件拖上积木、行内改对白/场景、底栏芯片加块、常驻试玩）；卡牌工作室表格化 + 行拖排序。
- 定案：之后通用底座 / 模板 / `kind` 一律按「小白也能做出可上线商业级作品」设计（写入 `MASTER_SPEC` / `ARCHITECTURE` / `editor-vision`）。
- 下线接水果/躲避/打地鼠小游戏模板：移除 `mini_game` 创建入口；工坊页重定向到作品编辑器。产品只保留可上线的小说与卡牌。
- 方案 A：游戏工坊并入作品编辑器。新建 `mini_game` 类型；幻想入口只留编辑器；旧 `game-workshop` 重定向；`game-studio` 降为模板工坊页。
- 编辑器增加「参考样品」：路边小说、地牢挂机（MIT idle-game-eb 职业/层数）、四人羁绊、战后三选一（MIT card-auto-battler 流程）。只填积木表，不搬引擎与立绘。
- 卡牌工作室按积木编辑：角色 / 关卡 / 羁绊。挂机连打关卡；修仙玩法指定几人上场触发羁绊加攻。数据表驱动，不搬 GPL 整仓。
- 卡牌拆成三种玩法（放置 / 排队技能 / 每局不同），编辑器收成一个「卡牌工作室」，面向小白：填角色和敌人就能播。
- 文档整理：新增总纲与分册（`MASTER_SPEC` / `ARCHITECTURE` / `FEATURES` / `UI_GUIDE` / `DATA_STRUCTURE` / `CHANGELOG`）；`archive/` 归档约定；Agent 默认先读总纲再按功能打开文件。不搬迁 `public/`、`src/`。

## 更早

作品云同步、卡牌 RPG、Companion、生命世界引擎、无限世界工作台等：见 `docs/history.md` 与仓库 git log。

## 怎么记

任务 commit 后：本文件加一行日期+摘要；细节段落追加 `history.md`。不要把长验证日志写进总纲。
